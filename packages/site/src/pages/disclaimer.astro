---
/** Legal disclaimer / attorney-advertising notice — words in
 * content/pages/disclaimer.md. noindex (set in the content file)
 * until an attorney reviews it. */
import Page from "../layouts/Page.astro";
import { pageContent } from "../lib/pages";

const { data: page, Content } = await pageContent("disclaimer");
---

<Page page={page} Content={Content} />
