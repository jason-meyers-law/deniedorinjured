---
/** Phase 2 silo hub (PLAN.md §33): the audience here is insureds whose
 * carrier denied or limited protection — a different reader than the
 * injury pages. Reserved now with a substantive overview (words in
 * content/pages/insurance-denied.md); the child pages (denied defense,
 * reservation of rights, bad faith, ...) come after the PI foundation
 * is established. */
import Page from "../../layouts/Page.astro";
import { pageContent } from "../../lib/pages";

const { data: page, Content } = await pageContent("insurance-denied");
---

<Page page={page} Content={Content} />
