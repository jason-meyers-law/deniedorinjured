---
/** Privacy policy — words in content/pages/privacy.md. Describes the
 * site's ACTUAL data practices only (GA4 analytics; the Google Maps
 * embed on the contact page; phone/email/form contact). noindex (set
 * in the content file) until an attorney reviews and sets it live. */
import Page from "../layouts/Page.astro";
import { pageContent } from "../lib/pages";

const { data: page, Content } = await pageContent("privacy");
---

<Page page={page} Content={Content} />
